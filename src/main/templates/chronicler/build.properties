# Generated at build time -- do not edit. See build.gradle's build stamp block.
# Namespaced under /chronicler/ so it cannot collide with another mod's copy.
commit=${build_commit}
branch=${build_branch}
time=${build_time}
version=${mod_version}+mc${minecraft_version}
